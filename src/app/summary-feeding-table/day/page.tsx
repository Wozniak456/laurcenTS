import { getPageTitle } from "@/utils/pageTitle";

export const metadata = {
  title: getPageTitle("Годування (дні)"),
};

export default function SummaryFeedingTableDayPage() {
  return (
    <div>
      <h1>Годування (дні)</h1>
      {/* Add your page content here */}
    </div>
  );
}
